# Decisions Log

Lightweight decision record (ADR-lite). Log decisions as they're made, with rationale — including small ones — so implementation choices don't outrun documented reasoning.

## Decision Log

| Date | Decision | Rationale | Status |
|---|---|---|---|
| 2026-07-07 | Established initial documentation set (`project-vision.md`, `current-focus.md`, `decisions.md`) before further implementation | Project had no recorded vision, audience, or design direction; further UI work risked compounding undocumented, ad-hoc choices | Accepted |
| 2026-07-07 | Fixed page `<title>` metadata from default "Create Next App" to project name | Hygiene fix, not a design decision — the literal string was inaccurate and didn't require vision input to correct | Accepted |
| 2026-07-07 | Finalized flagship project shortlist (5): Restore for Retail (2024), ACEM CPD Portal (2023), Goodwork (2021), MYER (2020), VicSuper (2018) | Resolves `homepage-strategy.md` Next Steps item 2 — each has real, verifiable outcome metrics and together they balance recency, rigor, and leadership signal; scored against all 11 live case studies | Accepted |
| 2026-07-07 | Open Universities Australia (OUA): keep on the site, replace the full case study with a shortened summary | OUA is NDA-blocked with no accessible case study — currently a dead link on the top-billed project. Dropping it loses brand-recognition value; a full sanitized rewrite wasn't chosen because the NDA's actual disclosure boundaries aren't yet scoped. A shortened summary is a middle path pending that scoping | Accepted |
| 2026-07-08 | Updated `project-vision.md` target roles: dropped "Lead Product Designer," replaced with Senior UX/UI and Senior Product Designer roles including practice-level design leadership (squad/small-org ownership, e.g. ANZ, Goodwork) | Surfaced while drafting the "Design/Product Leader" persona — "Lead Product Designer" reads as a people-management title, but the actual target is IC-track seniority with project/practice leadership, not team management. Left uncorrected, this would have skewed homepage/case-study framing toward the wrong kind of leadership signal | Accepted |
| 2026-07-08 | Brand Identity work (Rebel Lemming, The Neuropsychology Practice): kept as a lightweight standalone section, separate from the main UX/product case studies | Live-site audit (superseding the earlier undiscovered assumption) found real, substantive content — Rebel Lemming has a full case study — so dropping it outright was not warranted. Kept small/separate rather than folded into the main flagship/secondary tiers since brand identity is a distinct skill set from the UX/product work those tiers represent | Accepted |
| 2026-07-08 | Design the homepage before `design-system.md` — visual system will be extracted from homepage decisions rather than defined upfront | Deliberate reversal of the default sequencing in `homepage-strategy.md`'s Next Steps. Trade-off accepted: type/color/spacing choices made for the homepage become the de facto system and carry more weight than if constraints were set first | Accepted |
| 2026-07-08 | Finalized flagship homepage order: ACEM CPD Portal → Restore for Retail → Goodwork → MYER → VicSuper | Hand-curated narrative arc, not chronological or pure-metric: opens with the strongest complexity/rigor signal (Recruiter-persona credibility in the first seconds), resolves the recency question early (position 2) rather than saving it for last, surfaces leadership evidence once the Design/Product Leader persona is still reading (position 3), then craft-and-impact (MYER), closing on VicSuper's App Store rating — the single most universally legible metric on the site | Accepted |
| 2026-07-08 | Removed `nda-summary` as a project tier; NDA is now an independent `nda: boolean` flag, rendered as a badge on the project card/case study page rather than a distinct tier or menu-level category. OUA becomes `tier: 'secondary', nda: true` | Dedicating a whole tier value (and "visually distinct as NDA" special-case rendering) to a single project was over-engineering for n=1. A boolean flag is simpler, still explains OUA's short content via the badge, and is more extensible — any project, including a future flagship one, could carry the flag without needing a new tier | Accepted |
| 2026-07-08 | Added a client/company logo strip to the homepage (ANZ, MYER, VicSuper, Goodwork, ACEM, Restore), placed between the hero and the flagship case studies | Closes a gap `docs/homepage-strategy.md`'s original audit explicitly named — "zero social proof — no testimonials, no client logos" — that was never turned into an actual IA decision until surfaced during Stage 1 wireframe review. Kept deliberately quiet/compact so it doesn't compete with the hero or dilute the flagship section's dominance. OUA excluded (NDA); logo assets still need sourcing before real implementation | Accepted |
| 2026-07-08 | Clarified the homepage's "AI-in-Design / leadership proof-point" section: it's specifically an **AI-in-Design teaser** (headline, hook, link to `/ai-in-design`). "Leadership proof point" was dropped from the label — that job is done by leadership/systems-thinking signal woven into the flagship case studies above it (Goodwork's design-system governance, ANZ's squad context), not by separate content in this band | The compound label came from `homepage-strategy.md`'s Concept 3 fold-in language ("surface the AI-in-Design POV and leadership signals as supporting proof points"), which got carried into `content-strategy.md`, `information-architecture.md`, and the wireframe as one section without ever specifying what content actually populated the "leadership" half. Surfaced when asked directly what the section contained — worth resolving before Stage 2 (content/copy) writes real copy for it | Accepted |
| 2026-07-09 | ~~"The Projection" documented as explored future direction, not committed~~ **Superseded same day** — see below | — | Superseded |
| 2026-07-09 | **"The Projection" is now the committed homepage hero direction**, reversing the entry above. Backed by two detailed reference materials: a general visuals+motion workflow guide (which turns out to be the actual source of the Projection concept's example) and a fully-realized 10-frame storyboard + production brief (exact timings, rendered mockup, committed tech approach — Next.js + Tailwind + Framer Motion, real room photo as base layer, CSS blend modes for the projection effect). Supersedes `docs/homepage-wireframe.md`'s text-forward/no-imagery hero premise and deprioritizes the 3 typographic storyboard concepts | Reversal made with the earlier decision's own stated conditions largely satisfied: the reference brief supplies a concrete duration budget (interactive by 2.6–3.4s), and `docs/interaction-language.md` §9 already mandates a `prefers-reduced-motion` fallback regardless. Three details in the reference mockup are explicitly *not* adopted as decisions: the "Work About Process Contact" nav (conflicts with the established 3-item nav), the illustrative hero copy (real copy is still Stage 2, not started), and the "Selected Work" grid sketch (conflicts with the decided flagship list treatment) | Accepted |
| 2026-07-09 | Stage 3 (Key Visuals) marked done: `public/images/backgroundimage-light.png` (1536×1024) is the committed hero base layer — composition matches the brief closely (empty center wall for the projection, guitar right, amp/plant/art left, a dark object beside the amp reading naturally as the projector). Moving to Stage 4 (Motion Direction) on this asset | The specific guitar/amp/art shown are stand-ins, not literal matches for the real photos in `public/images/` (guitar, amp, lampshade, framed-artA/B) — whatever produced this treated those photos as style reference rather than doing a literal composite. Accepted as "a bit closer if possible" rather than a hard blocker — logged as an open refinement, not redone now, since the mood/palette/composition already satisfy the brief and re-doing it would stall Stage 4 | Accepted (with open refinement noted below) |
| 2026-07-10 | ~~Stage 2 copy finalized numbers-led (hero: "I turn ambiguous problems... cutting process steps by 30%, lifting feature adoption to 75%...")~~ **Superseded same day** — see below | Chosen at the time as the most direct application of Principle 1, "Results Earn the Read" — three real, checkable numbers in the first sentence. Superseded not because this reasoning was wrong, but because you decided you want the homepage to lead with process/craft/personality instead | Superseded |
| 2026-07-10 | **Stage 2 copy revised to lead with process, craft, and personality instead of impact numbers.** New hero: role label ("Senior Product Designer") + "I've spent a decade turning complicated, high-stakes problems — regulated compliance, healthcare, finance — into products people don't have to think twice about. It's the same instinct I started with painting murals and shaping skateboard decks: make something considered enough that it just works." All 5 flagship teasers reframed narratively (see `docs/content-strategy.md`) | You explicitly did not want a verbatim revert to the old site's audited-weakness sentence — this is new copy in a similar warm register, not a reuse of that exact line. Distinctiveness preserved by folding the creative-origin hook directly into the philosophy statement (rather than a generic passion sentence) and keeping real project specifics (regulated compliance, healthcare, finance) rather than vague claims. Underlying metrics aren't removed from the site — they still belong on case-study pages; this is about what the homepage leads with | Accepted |

## Open Decisions Needing Input

- **Heading color (`text-red-600` in `src/app/page.tsx`)** — chosen ad hoc with no design-system or brand rationale behind it. Revisit deliberately once `design-system.md` exists; it may end up red again, but for a reasoned reason.
- **Key Visual gear fidelity** — `backgroundimage-light.png` uses a stand-in guitar, amp, and art print rather than the real ones in `public/images/` (guitar, amp, lampshade, framed-artA/B). Accepted for now to keep Stage 4 moving; revisit if it's worth commissioning a closer match before final implementation.
- **Page `<meta description>`** — still reads "Generated by create next app." Left as-is deliberately: unlike the title, this is positioning-driven copy and should wait for `project-vision.md`.

## Inherited / Default Decisions

*Choices made implicitly by `create-next-app` scaffolding, not yet deliberate — flagged for reconsideration, not treated as settled.*

- Next.js App Router (vs. Pages Router)
- Tailwind CSS v4, CSS-first config (no `tailwind.config.js`)
- Geist / Geist Mono fonts
- TypeScript strict mode
- Default Vercel deployment target (untouched from scaffold)

## Superseded Decisions

- **2026-07-09** — "The Projection" as an explored-but-not-committed future direction. Superseded same day once reference materials with a concrete production/timing/tech plan were provided; see the Decision Log for the reversal entry.
- **2026-07-10** — Stage 2 homepage copy finalized numbers-led (hero led with three impact percentages). Superseded same day in favor of process/craft/personality-led copy; see the Decision Log for both versions and the reasoning behind each.
